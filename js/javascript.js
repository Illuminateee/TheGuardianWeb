function scrollToHome(){
    let targetElement = document.getElementById('home-content');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}
function scrollToTes(){
    let targetElement = document.getElementById('testimonial-content');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}
function scrollToAbout(){
    let targetElement = document.getElementById('about-content');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}